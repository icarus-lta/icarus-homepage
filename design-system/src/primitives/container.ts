/**
 * The page gutter. Content lines up this far in from the window edge, so text sits with the
 * same margin as the full-bleed photography instead of being boxed into a narrow centre column.
 * Use it on any band you build yourself: `<div className={containerClass}>`.
 */
export const containerClass = 'w-full mx-auto px-6 md:px-10 lg:px-16 2xl:px-24';
